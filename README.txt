For running the project first open the backend part of the project and in the terminal type
python manage.py runserver
Afterwards open the frontend part and type in the terminal npm run dev
Then in a browser enter the address http://localhost:3000 or http://192.168.1.129:3000/
both the frontend and backend can be opened in either PyCharm or vscode